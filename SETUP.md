# Setup

```bash
git init
git config user.name 'kvnlnk'
git config user.email 'kvnlnk@users.noreply.github.com'

# Already initialized — just install
npm install
```

## Development
```bash
npm run dev    # Vite dev server
npm run build  # Production build to dist/
```
